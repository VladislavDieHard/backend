version: '3.8'

services:
  web:
    build: .
    image: vladdamage/infomania_backend:v2
    container_name: web
    restart: always
    ports:
      - '3333:3000'
    env_file:
      - .production.env


